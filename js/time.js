function time() {    window.setTimeout("time()", 1000);    var seconds = 1000;    var minutes = seconds * 60;    var hours = minutes * 60;    var days = hours * 24;    var years = days * 365;    var today = new Date();    var todayYear = today.getFullYear();    var todayMonth = today.getMonth() + 1;    var todayDate = today.getDate();    var todayHour = today.getHours();    var todayMinute = today.getMinutes();    var todaySecond = today.getSeconds();    var targetYear = 2023;    var targetMonth = 1;    var targetDate = 22;    var targetHour = 0;    var targetMinute = 0;    var targetSecond = 0;    var t1 = Date.UTC(targetYear, targetMonth, targetDate, targetHour, targetMinute, targetSecond);    var t2 = Date.UTC(todayYear, todayMonth, todayDate, todayHour, todayMinute, todaySecond);    var diff = t1 - t2;    if (diff >= 0) {        document.getElementById("time").innerHTML = "离癸卯年还有" + Math.floor(diff / days) + "天" + Math.floor((diff / hours) - Math.floor(diff / days) * 24) + "小时" + Math.floor((diff - (Math.floor(diff / days) * days + Math.floor((diff / hours) - Math.floor(diff / days) * 24) * hours)) / minutes) + "分钟" + Math.floor((diff - (Math.floor(diff / days) * days + Math.floor((diff / hours) - Math.floor(diff / days) * 24) * hours + Math.floor((diff - (Math.floor(diff / days) * days + Math.floor((diff / hours) - Math.floor(diff / days) * 24) * hours)) / minutes) * minutes)) / seconds) + "秒";    } else {        document.getElementById("time").innerHTML = "新年快乐";    }}time();